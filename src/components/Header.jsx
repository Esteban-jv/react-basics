import React from "react";

function Header({titulo}) {
    /* logica JavaScript */

    return (
        /* lo que se ve en pantalla */
        /* para css en lugar de la directiva class es className */
        <h1>{titulo}</h1>
    )
}

export default Header;